var React = require('react');
var ReactDOMServer = require('react-dom/server');

function Hello() {
    return // Place your compiled code here.
}

ReactDOMServer.renderToString(
    React.createElement(Hello)
);